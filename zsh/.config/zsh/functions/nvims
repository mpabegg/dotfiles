local items=("default" "LazyVim")
local config=$(printf "%s\n" "${items[@]}" | fzf --prompt=" Nvim Distro >> " --height=~50% --layout=reverse --border --exit-0)

if [[ -z $config ]]; then
  return 0;
elif [[ $config == "default" ]]; then
  config=""
fi

NVIM_APPNAME=$config nvim $@
