unbind-key C-b
set-option -g prefix C-Space
bind-key C-Space send-prefix

set-option -g detach-on-destroy off

# Fix colors
set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color*:Tc"

# Fix delay on Escape
set -s escape-time 0

# Start numbering windows from index 1
set -g base-index 1

set-option -g history-limit 10000

# Reload config, much better than having to restart TMUX every time
bind r "source-file ~/.config/tmux/tmux.conf; display-message 'Reloaded config'"
# Clear screen and scroll-back buffer
bind k send-keys -R Enter\; clear-history

bind-key s run-shell -b "~/.local/bin/tmux_switch_or_create.sh"

# Use <C-b>w as prefix for pane/operations
bind-key w "switch-client -Tpane-binds"

bind-key -Tpane-binds v splitw -h
bind-key -Tpane-binds s splitw
bind-key -Tpane-binds d killp
bind-key -Tpane-binds h "select-pane -L"
bind-key -Tpane-binds j "select-pane -D"
bind-key -Tpane-binds k "select-pane -U"
bind-key -Tpane-binds l "select-pane -R"
bind-key -Tpane-binds w "choose-window"
bind-key -Tpane-binds n "new-window"

is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?)(diff)?$'"
bind-key -Troot C-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
bind-key -Troot C-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
bind-key -Troot C-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
bind-key -Troot C-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"

set-option -g mouse on

## COLORSCHEME: everforest dark low
set -g @everforest_bg_dim '#293136'
set -g @everforest_bg0 '#333c43'
set -g @everforest_bg1 '#3a464c'
set -g @everforest_bg2 '#434f55'
set -g @everforest_bg3 '#4d5960'
set -g @everforest_bg4 '#555f66'
set -g @everforest_bg5 '#5d6b66'
set -g @everforest_bg_visual '#5c3f4f'
set -g @everforest_bg_red '#59464c'
set -g @everforest_bg_green '#48584e'
set -g @everforest_bg_blue '#3f5865'
set -g @everforest_bg_yellow '#55544a'

set -g @everforest_fg '#d3c6aa'
set -g @everforest_red '#e67e80'
set -g @everforest_orange '#e69875'
set -g @everforest_yellow '#dbbc7f'
set -g @everforest_green '#a7c080'
set -g @everforest_aqua '#83c092'
set -g @everforest_blue '#7fbbb3'
set -g @everforest_purple '#d699b6'
set -g @everforest_grey0 '#7a8478'
set -g @everforest_grey1 '#859289'
set -g @everforest_grey2 '#9da9a0'
set -g @everforest_statusline1 '#a7c080'
set -g @everforest_statusline2 '#d3c6aa'
set -g @everforest_statusline3 '#e67e80'

set -g status-position top
set-option -g status "on"
set -g status-interval 2

set-option -g status-fg '#d3c6aa' # fg (No idea why I can't use variables here)
set-option -g status-bg '#333c43' # bg0

set-option -g mode-style fg='#{@everforest_purple}',bg='#{@everforest_bg_red}' # fg=purple, bg=bg_visual

# default statusbar colors
set-option -g status-style fg='#{@everforest_fg}',bg='#{@everforest_bg_dim}',default # fg=fg bg=bg_dim

# ---- Windows ----
# default window title colors
set-window-option -g window-status-style fg='#{@everforest_bg5}',bg='#{@everforest_bg0}' # fg=yellow bg=bg0

# default window with an activity alert
set-window-option -g window-status-activity-style 'bg=#{@everforest_bg1},fg=#{@everforest_bg3}' # bg=bg1, fg=fg3

# active window title colors
set-window-option -g window-status-current-style fg='#{@everforest_fg}',bg='#{@everforest_bg_green}' # fg=fg bg=bg_green

# ---- Pane ----
# pane borders
set-option -g pane-border-style fg='#{@everforest_bg1}' # fg=bg1
set-option -g pane-active-border-style 'fg=#{@everforest_blue}' # fg=blue

# pane number display
set-option -g display-panes-active-colour '#7fbbb3' # blue
set-option -g display-panes-colour '#e69875' # orange

# ---- Command ----
# message info
set-option -g message-style fg='#{@everforest_statusline3}',bg='#{@everforest_bg_dim}' # fg=statusline3 bg=bg_dim

# writing commands inactive
set-option -g message-command-style 'fg=#{@everforest_bg3},bg=#{@everforest_bg1}' # bg=fg3, fg=bg1

# bell
set-window-option -g window-status-bell-style fg='#{@everforest_bg0}',bg='#{@everforest_statusline3}' # fg=bg, bg=statusline3

# ---- Formatting ----
set-option -g status-left-style none
set -g status-left-length 60
set -g status-left '#[fg=#{@everforest_bg_dim},bg=#{@everforest_green},bold] #S  #[fg=#{@everforest_green},bg=#{@everforest_bg0},bold]  '

set-option -g status-right-style none
set -g status-right-length 150
set -g status-right '#[fg=#{@everforest_fg},bg=#{@everforest_bg2}] %Y-%m-%d  %H:%M #[fg=#{@everforest_bg_dim},bg=#{@everforest_aqua},bold]  #{b:pane_current_path} '

set -g window-status-format "#[fg=#{@everforest_grey0},bg=#{@everforest_bg2}] #I #{?window_zoomed_flag,󰁌,} #[fg=#{@everforest_grey0},bg=#{@everforest_bg2}]#W "
set -g window-status-current-format "#[fg=#{@everforest_fg},bg=#{@everforest_bg_green}] #I #{?window_zoomed_flag,󰁌,} #[fg=#{@everforest_fg},bg=#{@everforest_bg_green},bold]#W "

# #### THEMING ####
# #### This is stolen from https://github.com/catppuccin/tmux
#
# # thm_bg="#303446"
# # thm_fg="#c6d0f5"
# # thm_cyan="#99d1db"
# # thm_black="#292c3c"
# # thm_gray="#414559"
# # thm_magenta="#ca9ee6"
# # thm_pink="#f4b8e4"
# # thm_red="#e78284"
# # thm_green="#a6d189"
# # thm_yellow="#e5c890"
# # thm_blue="#8caaee"
# # thm_orange="#ef9f76"
# # thm_black4="#626880"
#
# sep_left=""
# sep_right=""
#
# set -g renumber-windows on
# set -g status-position top
# set -g status "on"
# set -g status-bg "${thm_black}"
# set -g status-justify "left"
# set -g status-left-length "100"
# set -g status-right-length "100"
#
# # messages
# set -g message-style "fg=${thm_cyan},bg=${thm_gray},align=centre"
# set -g message-command-style "fg=${thm_cyan},bg=${thm_gray},align=centre"
#
# # panes
# set -g pane-border-style "fg=${thm_gray}"
# set -g pane-active-border-style "fg=${thm_blue}"
#
# session_color="#{?client_prefix,$thm_red,$thm_green}"
# set -g status-left " #[fg=$thm_black bg=$session_color]$sep_left #S  $sep_right#[fg=$thm_pink bg=$thm_black] "
# set -g status-right "#[fg=$thm_black,bg=$thm_pink]$sep_left  #{b:pane_current_path} $sep_right#[fg=$thm_pink bg=$thm_black]"
#
# ## Current Window
# current_zoomed_status="#[fg=$thm_gray bg=$thm_orange]$sep_left #I #W 󰁌 #[fg=$thm_orange bg=$thm_black]$sep_right"
# current_status="#[fg=$thm_black bg=$thm_orange]$sep_left #I #[fg=$thm_orange bg=$thm_gray] #W $sep_right#[fg=$thm_gray bg=$thm_black]"
# set-window-option -gq window-status-current-format "#{?window_zoomed_flag,$current_zoomed_status,$current_status}"
#
# window_zoomed_status="#[fg=$thm_black bg=$thm_blue]$sep_left #I #W 󰁌 #[fg=$thm_blue bg=$thm_black]$sep_right "
# window_status="#[fg=$thm_black bg=$thm_blue]$sep_left #I #[fg=$thm_blue bg=$thm_gray] #W $sep_right#[fg=$thm_gray bg=$thm_black]"
# set-window-option -gq window-status-format "#{?window_zoomed_flag,$window_zoomed_status,$window_status}"

set -g @continuum-restore 'on'
set -g @continuum-boot 'on'
set -g @continuum-boot-options 'alacritty'

set -g @plugin tmux-plugins/tmux-resurrect
set -g @plugin tmux-plugins/tmux-continuum

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
set-environment -g TMUX_PLUGIN_MANAGER_PATH "$XDG_DATA_HOME/tmux/plugins/"
set -g @plugin 'tmux-plugins/tpm'
run "$XDG_DATA_HOME/tmux/plugins/tpm/tpm"
